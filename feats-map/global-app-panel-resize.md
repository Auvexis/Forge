# Global app panel resize

## Tasks
- [x] Add contract coverage for global panel resize props and execution panel top resize config.
- [x] Extend app panel store/config to carry `resizable` and `resizeSide`.
- [x] Implement resize handles in `AppPanel.vue` for top/bottom/left/right.
- [x] Pass resize props through `GlobalAppPanel.vue`.
- [x] Configure `ExecutionBottomPanel.vue` usage to resize from the top.
- [x] Run focused verification and commit.
