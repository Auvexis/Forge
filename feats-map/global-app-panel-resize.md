# Global app panel resize

## Tasks
- [x] Add contract coverage for global panel resize props and execution panel top resize config.
- [ ] Extend app panel store/config to carry `resizable` and `resizeSide`.
- [ ] Implement resize handles in `AppPanel.vue` for top/bottom/left/right.
- [ ] Pass resize props through `GlobalAppPanel.vue`.
- [ ] Configure `ExecutionBottomPanel.vue` usage to resize from the top.
- [ ] Run focused verification and commit.
