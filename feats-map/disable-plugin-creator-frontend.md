# Disable Plugin Creator Frontend

- [x] Remove Plugin Creator from visible sidebar navigation.
- [x] Disable `/plugin-creator` and `/plugin-creator/:pluginId` from loading the frontend page.
- [x] Keep build/type-check passing without deleting backend or shared creator code.
