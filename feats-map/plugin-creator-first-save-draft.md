# Plugin Creator First Save Draft

- [x] Stop auto-creating a backend plugin when opening an empty plugin creator workspace.
- [x] Keep a local unsaved blueprint so the canvas can receive nodes before metadata exists.
- [x] Open the metadata BaseModal on the first save, then create and persist the plugin.
- [x] Verify with focused frontend tests.
