version: '3.8'

services:
  nod8-server:
    build: 
      context: ./server
      dockerfile: Dockerfile
    container_name: nod8-server
    ports:
      - "23801:23801"
    environment:
      - ND8_HOME=/app/.nd8
    volumes:
      - nd8_data:/app/.nd8
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:23801/"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always

volumes:
  nd8_data:

  nod8-client:
    build: 
      context: ./client
      dockerfile: Dockerfile
    container_name: nod8-client
    ports:
      - "23802:23802"
    environment:
      - VITE_API_BASE_URL=http://localhost:23801
    depends_on:
      nod8-server:
        condition: service_healthy
    restart: always
